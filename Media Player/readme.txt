The python script is made completely using AI
The arduino code sends to serial whenever a button is pressed either b, p, f, or v
Admittedly v is an extra button I couldnt find a use for it could be easily removed.
Python script has to run in the background to read these letters and then it uses keyboard shortcuts to prev - pause/play - next
